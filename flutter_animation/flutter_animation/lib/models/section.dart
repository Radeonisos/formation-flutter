import 'package:flutter/material.dart';

class Section {
  String name;
  IconData icon;
  Widget destination;

  Section(
      {@required this.name, @required this.icon, @required this.destination});
}
